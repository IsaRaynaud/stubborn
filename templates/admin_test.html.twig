<!DOCTYPE html>
<html>
<head>
    <title>Form Test</title>
</head>
<body>
    <h1>Test Formulaire Produit (Sans Layout)</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Test Submit</button>
    {{ form_end(form) }}
</body>
</html>
